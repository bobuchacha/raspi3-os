# Tools

This directory is reserved for image builders, flashing helpers, emulation wrappers, symbol inspection helpers, and host-side validation scripts.

Current active tooling includes `tools/my-loader/ldr_build.py`, which packs the linked higher-half kernel ELF into the packed `kernel` image used by the second-stage bootloader.
